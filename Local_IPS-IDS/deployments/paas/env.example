# Pandora Box Console IDS-IPS 環境變數配置範例
# 複製此檔案為 .env 並填入實際值

# =============================================================================
# OCI (Oracle Cloud Infrastructure) 配置
# =============================================================================
OCI_USER=ocid1.user.oc1..your-user-ocid
OCI_TENANCY=ocid1.tenancy.oc1..your-tenancy-ocid
OCI_REGION=us-ashburn-1
OCI_FINGERPRINT=your-api-key-fingerprint
OCI_KEY_FILE=/path/to/your/private-key.pem
OCI_NAMESPACE=your-oci-namespace
CLUSTER_OCID=ocid1.cluster.oc1..your-cluster-ocid

# OCI Container Registry 認證
OCI_USERNAME=your-oci-namespace/your-user
OCI_PASSWORD=your-auth-token

# =============================================================================
# GCP (Google Cloud Platform) 配置
# =============================================================================
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCP_ZONE=us-central1-a
GCP_CLUSTER_NAME=pandora-cluster
GCP_SA_KEY=/path/to/your/service-account-key.json

# GCP Container Registry (GCR)
GCR_REGISTRY=gcr.io/your-gcp-project-id

# =============================================================================
# Kubernetes 配置
# =============================================================================
KUBECONFIG=~/.kube/config
NAMESPACE=pandora-box

# =============================================================================
# 資料庫配置
# =============================================================================
# PostgreSQL
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=pandora
POSTGRES_USER=pandora
POSTGRES_PASSWORD=pandora123

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=pandora123

# =============================================================================
# 應用程式配置
# =============================================================================
# Agent 配置
AGENT_LOG_LEVEL=info
AGENT_DEVICE_PORT=/dev/ttyUSB0
AGENT_TIMEOUT=30m
AGENT_BLOCK_TIME=20:00
AGENT_UNLOCK_TIME=08:00

# Console 配置
CONSOLE_PORT=3001
CONSOLE_LOG_LEVEL=info
CONSOLE_JWT_SECRET=your-jwt-secret-key-here

# UI 配置
UI_PORT=3001
UI_API_BASE_URL=http://localhost:3001/api

# =============================================================================
# 監控系統配置
# =============================================================================
# Prometheus
PROMETHEUS_URL=http://prometheus:9090
PROMETHEUS_RETENTION=30d

# Grafana
GRAFANA_URL=http://grafana:3000
GRAFANA_ADMIN_PASSWORD=pandora123
GRAFANA_SECRET_KEY=your-grafana-secret-key

# Loki
LOKI_URL=http://loki:3100

# =============================================================================
# 安全配置
# =============================================================================
# mTLS 憑證路徑
MTLS_CERT_PATH=/certs/client.crt
MTLS_KEY_PATH=/certs/client.key
MTLS_CA_PATH=/certs/ca.crt

# 預設認證金鑰
DEFAULT_PSK=pandora-default-key

# =============================================================================
# Vercel 配置
# =============================================================================
VERCEL_TOKEN=your-vercel-token
VERCEL_ORG_ID=your-vercel-org-id
VERCEL_PROJECT_ID=your-vercel-project-id

# 前端環境變數 (OCI 環境)
VERCEL_API_BASE_URL_OCI=https://pandora-oci.yourdomain.com/api
VERCEL_GRAFANA_URL_OCI=https://pandora-oci.yourdomain.com/grafana
VERCEL_PROMETHEUS_URL_OCI=https://pandora-oci.yourdomain.com/prometheus

# 前端環境變數 (GCP 環境)
VERCEL_API_BASE_URL_GCP=https://pandora-gcp.yourdomain.com/api
VERCEL_GRAFANA_URL_GCP=https://pandora-gcp.yourdomain.com/grafana
VERCEL_PROMETHEUS_URL_GCP=https://pandora-gcp.yourdomain.com/prometheus

# =============================================================================
# GitHub Actions 密鑰 (需要在 GitHub Repository Settings 中設定)
# =============================================================================
# 以下密鑰需要在 GitHub Repository -> Settings -> Secrets and variables -> Actions 中設定
#
# OCI 配置密鑰:
# OCI_USER
# OCI_TENANCY
# OCI_REGION
# OCI_FINGERPRINT
# OCI_KEY_FILE
# OCI_NAMESPACE
# CLUSTER_OCID
# OCI_USERNAME
# OCI_PASSWORD

# GCP 配置密鑰:
# GCP_PROJECT_ID
# GCP_CLUSTER_NAME
# GCP_SA_KEY

# Vercel 配置密鑰:
# VERCEL_TOKEN
# VERCEL_ORG_ID
# VERCEL_PROJECT_ID_OCI
# VERCEL_PROJECT_ID_GCP
# VERCEL_API_BASE_URL_OCI
# VERCEL_GRAFANA_URL_OCI
# VERCEL_PROMETHEUS_URL_OCI
# VERCEL_API_BASE_URL_GCP
# VERCEL_GRAFANA_URL_GCP
# VERCEL_PROMETHEUS_URL_GCP

# =============================================================================
# 開發環境配置
# =============================================================================
# 開發模式
NODE_ENV=development
GIN_MODE=debug

# 日誌配置
LOG_FORMAT=json
LOG_LEVEL=debug

# 測試配置
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=pandora_test
TEST_REDIS_HOST=localhost
TEST_REDIS_PORT=6379